name: Apply Free Plan UI Polish

on:
  push:
    branches:
      - development
    paths:
      - .github/workflows/apply-free-plan-ui-polish.yml

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Bootstrap
        run: echo ready
